<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Futuristic Portfolio</title>
<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Rajdhani:wght@400;600&display=swap" rel="stylesheet">
<style>
    body {
        margin: 0;
        font-family: 'Rajdhani', sans-serif;
        background: radial-gradient(circle at 20% 20%, #0a0a0a, #000000);
        color: #fff;
        overflow-x: hidden;
    }

    /* Landing Section */
    header {
        height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        position: relative;
        text-align: center;
    }

    h1 {
        font-family: 'Orbitron', sans-serif;
        font-size: 3rem;
        text-transform: uppercase;
        color: #00f0ff;
        text-shadow: 0 0 15px #00f0ff, 0 0 30px #007f8f;
        animation: glow 2s ease-in-out infinite alternate;
    }

    @keyframes glow {
        from {
            text-shadow: 0 0 10px #00f0ff, 0 0 20px #007f8f;
        }
        to {
            text-shadow: 0 0 20px #00f0ff, 0 0 40px #00cfff;
        }
    }

    h2 {
        font-size: 1.5rem;
        margin-top: 10px;
        color: #cceeff;
    }

    .coming-soon {
        margin-top: 20px;
        font-size: 1.2rem;
        padding: 10px 20px;
        border: 2px solid #00f0ff;
        border-radius: 25px;
        text-transform: uppercase;
        background: rgba(0, 240, 255, 0.1);
        color: #00f0ff;
        cursor: pointer;
        transition: all 0.3s ease;
    }

    .coming-soon:hover {
        background: #00f0ff;
        color: #000;
        box-shadow: 0 0 15px #00f0ff, 0 0 30px #00cfff;
    }

    /* Navigation */
    nav {
        position: absolute;
        top: 20px;
        right: 40px;
    }

    nav a {
        margin: 0 15px;
        color: #cceeff;
        text-decoration: none;
        font-size: 1.1rem;
        transition: 0.3s;
    }

    nav a:hover {
        color: #00f0ff;
        text-shadow: 0 0 10px #00f0ff;
    }

    /* Floating Elements */
    .floating-shape {
        position: absolute;
        width: 15px;
        height: 15px;
        background: #00f0ff;
        border-radius: 50%;
        animation: float 6s infinite ease-in-out;
    }

    @keyframes float {
        0%, 100% {
            transform: translateY(0);
        }
        50% {
            transform: translateY(-20px);
        }
    }

    /* Footer */
    footer {
        text-align: center;
        padding: 20px;
        font-size: 0.9rem;
        color: #888;
        background: #050505;
        border-top: 1px solid #111;
    }
</style>
</head>
<body>

<header>
    <nav>
        <a href="#home">Home</a>
        <a href="#about">About</a>
        <a href="#portfolio">Portfolio</a>
        <a href="#contact">Contact</a>
    </nav>

    <h1>John Doe</h1>
    <h2>Graphic Designer • Illustrator</h2>
    <div class="coming-soon">Welcome</div>

    <!-- Floating shapes -->
    <div class="floating-shape" style="top: 10%; left: 20%; animation-delay: 0s;"></div>
    <div class="floating-shape" style="top: 30%; left: 80%; animation-delay: 1s;"></div>
    <div class="floating-shape" style="top: 70%; left: 50%; animation-delay: 2s;"></div>
</header>

<footer>
    &copy; 2025 John Doe | All rights reserved
</footer>

</body>
</html>
